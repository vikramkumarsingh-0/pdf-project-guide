import { supabase } from '@/integrations/supabase/client';import type { MaterialDoc } from './recommendation';
export type Subject={id:string;name:string;category:string;description:string};
export async function getCatalog(){const[{data:materials,error},{data:subjects}]=await Promise.all([supabase.from('materials').select('*,subjects(name)').eq('approval_status','approved').order('average_rating',{ascending:false}),supabase.from('subjects').select('*').order('name')]);if(error)throw error;return{materials:(materials||[]) as MaterialDoc[],subjects:(subjects||[]) as Subject[]}}
export async function openMaterial(m:MaterialDoc,userId?:string){if(userId){await supabase.from('material_views').insert({user_id:userId,material_id:m.id});}if(m.file_path){const{data,error}=await supabase.storage.from('study-materials').createSignedUrl(m.file_path,300);if(!error&&data?.signedUrl){window.open(data.signedUrl,'_blank','noopener,noreferrer');return}}if(m.url)window.open(m.url,'_blank','noopener,noreferrer')}
