import { ExternalLink,FileText,PlayCircle,Newspaper,Star } from 'lucide-react';
import { Button } from '@/components/ui/button';import { Badge } from '@/components/ui/badge';import type { MaterialDoc } from '@/lib/recommendation';
const icons={PDF:FileText,Video:PlayCircle,Article:Newspaper};
export function MaterialCard({material,reason,onOpen}:{material:MaterialDoc;reason?:string;onOpen?:(m:MaterialDoc)=>void}){const Icon=icons[material.type];return <article className="material-card group"><div className="flex items-start justify-between gap-3"><span className={`type-icon type-${material.type.toLowerCase()}`}><Icon/></span><Badge variant="outline">{material.type}</Badge></div><div className="mt-5"><p className="eyebrow">{material.subjects?.name||'General studies'}</p><h3 className="mt-2 line-clamp-2 text-lg font-semibold leading-snug">{material.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{material.description}</p></div><div className="mt-4 flex flex-wrap gap-1.5">{material.tags.slice(0,3).map(t=><span className="tag" key={t}>{t}</span>)}</div>{reason&&<p className="mt-4 border-l-2 border-accent pl-3 text-xs font-medium text-muted-foreground">{reason}</p>}<div className="mt-5 flex items-center justify-between border-t border-border pt-4"><span className="flex items-center gap-1 text-sm font-medium"><Star className="size-4 fill-current text-rating"/>{Number(material.average_rating).toFixed(1)} <span className="text-muted-foreground">({material.rating_count})</span></span><Button size="sm" onClick={()=>onOpen?.(material)}>Open <ExternalLink/></Button></div></article>}
