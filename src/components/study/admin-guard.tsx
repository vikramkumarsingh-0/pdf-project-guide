import { useEffect,useState,type ReactNode } from 'react';import { Link } from '@tanstack/react-router';import { ShieldAlert } from 'lucide-react';import { supabase } from '@/integrations/supabase/client';import { Button } from '@/components/ui/button';
export function AdminGuard({userId,children}:{userId:string;children:ReactNode}){const[allowed,setAllowed]=useState<boolean|null>(null);useEffect(()=>{supabase.rpc('has_role',{_user_id:userId,_role:'admin'}).then(({data})=>setAllowed(Boolean(data)))},[userId]);if(allowed===null)return <div className="page-wrap"><div className="skeleton-block"/></div>;if(!allowed)return <div className="empty-state min-h-[70vh]"><ShieldAlert/><h1>Administrator access only</h1><p>Your account does not have permission to view this area.</p><Link to="/dashboard"><Button>Return to dashboard</Button></Link></div>;return <>{children}</>}
