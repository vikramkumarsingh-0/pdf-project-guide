import { createFileRoute,Link,useNavigate } from '@tanstack/react-router';import { useState } from 'react';import { toast } from 'sonner';import { ArrowRight,Chrome } from 'lucide-react';import { supabase } from '@/integrations/supabase/client';import { lovable } from '@/integrations/lovable';import { Brand } from '@/components/study/brand';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { Label } from '@/components/ui/label';
export const Route=createFileRoute('/login')({head:()=>({meta:[{title:'Sign in | StudyFlow AI'},{name:'description',content:'Sign in to your personalized StudyFlow AI learning space.'},{property:'og:title',content:'Sign in | StudyFlow AI'},{property:'og:description',content:'Access personalized study recommendations.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Login});
function Login(){const nav=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const{error}=await supabase.auth.signInWithPassword({email,password});setBusy(false);if(error)return toast.error(error.message);toast.success('Welcome back');nav({to:'/dashboard'})}async function google(){const r=await lovable.auth.signInWithOAuth('google',{redirect_uri:window.location.origin});if(r.error)toast.error(r.error.message);else if(!r.redirected)nav({to:'/dashboard'})}return <main className="auth-page"><Link to="/" className="absolute left-6 top-6"><Brand/></Link><section className="auth-panel"><p className="eyebrow">Your learning space</p><h1>Welcome back</h1><p className="text-muted-foreground">Pick up where you left off with recommendations shaped around you.</p><form onSubmit={submit} className="mt-8 space-y-5"><div><Label htmlFor="email">Email</Label><Input id="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required className="mt-2 h-11"/></div><div><Label htmlFor="password">Password</Label><Input id="password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} className="mt-2 h-11"/></div><Button className="h-11 w-full" disabled={busy}>{busy?'Signing in…':<>Sign in <ArrowRight/></>}</Button></form><div className="my-6 flex items-center gap-3 text-xs text-muted-foreground"><span className="h-px flex-1 bg-border"/>OR<span className="h-px flex-1 bg-border"/></div><Button variant="outline" className="h-11 w-full" onClick={google}><Chrome/>Continue with Google</Button><p className="mt-6 text-center text-sm text-muted-foreground">New here? <Link to="/register" className="font-semibold text-primary">Create an account</Link></p></section></main>}
