import { createFileRoute,Link,useNavigate } from '@tanstack/react-router';import { useEffect,useState } from 'react';import { toast } from 'sonner';import { ArrowRight,Check } from 'lucide-react';import { supabase } from '@/integrations/supabase/client';import { Brand } from '@/components/study/brand';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { Label } from '@/components/ui/label';import type { Subject } from '@/lib/catalog';
export const Route=createFileRoute('/register')({head:()=>({meta:[{title:'Create account | StudyFlow AI'},{name:'description',content:'Create your StudyFlow AI student account.'},{property:'og:title',content:'Join StudyFlow AI'},{property:'og:description',content:'Start your personalized learning journey.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Register});
function Register(){const nav=useNavigate();const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[subjects,setSubjects]=useState<Subject[]>([]);const[selected,setSelected]=useState<string[]>([]);useEffect(()=>{supabase.from('subjects').select('*').order('name').then(({data})=>setSubjects(data||[]))},[]);async function submit(e:React.FormEvent){e.preventDefault();if(password.length<8)return toast.error('Use at least 8 characters');const{data,error}=await supabase.auth.signUp({email,password,options:{data:{name}}});if(error)return toast.error(error.message);if(data.user&&data.session&&selected.length)await supabase.from('enrollments').insert(selected.map(subject_id=>({user_id:data.user.id,subject_id})));toast.success(data.session?'Account created':'Check your email to confirm your account');nav({to:data.session?'/dashboard':'/login'})}return <main className="auth-page py-24"><Link to="/" className="absolute left-6 top-6"><Brand/></Link><section className="auth-panel max-w-2xl"><p className="eyebrow">Begin with your interests</p><h1>Create your student profile</h1><form onSubmit={submit} className="mt-7 space-y-5"><div className="grid gap-4 sm:grid-cols-2"><div><Label htmlFor="name">Full name</Label><Input id="name" value={name} onChange={e=>setName(e.target.value)} required minLength={2} className="mt-2 h-11"/></div><div><Label htmlFor="reg-email">Email</Label><Input id="reg-email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required className="mt-2 h-11"/></div></div><div><Label htmlFor="reg-password">Password</Label><Input id="reg-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} className="mt-2 h-11"/></div><fieldset><legend className="text-sm font-medium">Choose enrolled subjects</legend><div className="mt-3 grid gap-2 sm:grid-cols-2">{subjects.map(s=><button type="button" key={s.id} onClick={()=>setSelected(x=>x.includes(s.id)?x.filter(i=>i!==s.id):[...x,s.id])} className={`subject-choice ${selected.includes(s.id)?'selected':''}`}><span>{s.name}</span>{selected.includes(s.id)&&<Check/>}</button>)}</div></fieldset><Button className="h-11 w-full">Create account <ArrowRight/></Button></form><p className="mt-6 text-center text-sm text-muted-foreground">Already registered? <Link to="/login" className="font-semibold text-primary">Sign in</Link></p></section></main>}
