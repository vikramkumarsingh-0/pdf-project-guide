import { createFileRoute,Link } from '@tanstack/react-router';import { useEffect,useState } from 'react';import { BookOpen,ChartNoAxesCombined,LibraryBig,Plus,Search,Star,Users } from 'lucide-react';import { supabase } from '@/integrations/supabase/client';import { AdminGuard } from '@/components/study/admin-guard';import { Button } from '@/components/ui/button';
export const Route=createFileRoute('/_authenticated/admin/dashboard')({head:()=>({meta:[{title:'Admin dashboard | StudyFlow AI'},{name:'description',content:'StudyFlow administration and system overview.'},{property:'og:title',content:'Admin dashboard | StudyFlow AI'},{property:'og:description',content:'Manage learning resources and insights.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Admin});
function Admin(){const{user}=Route.useRouteContext();const[stats,setStats]=useState({materials:0,subjects:0,ratings:0,searches:0});useEffect(()=>{Promise.all(['materials','subjects','ratings','search_logs'].map(t=>supabase.from(t as any).select('*',{count:'exact',head:true}))).then(x=>setStats({materials:x[0].count||0,subjects:x[1].count||0,ratings:x[2].count||0,searches:x[3].count||0}))},[]);return <AdminGuard userId={user.id}><div className="page-wrap"><div className="page-title flex-row"><div><p className="eyebrow">Administration</p><h1>System overview</h1><p>Manage the catalog and monitor learning activity.</p></div><Link to="/admin/materials"><Button><Plus/>Add material</Button></Link></div><div className="stats-grid">{[[LibraryBig,stats.materials,'Materials'],[BookOpen,stats.subjects,'Subjects'],[Star,stats.ratings,'Ratings'],[Search,stats.searches,'Searches']].map(([Icon,n,l])=><article className="stat" key={String(l)}><span><Icon/></span><div><b>{String(n)}</b><p>{String(l)}</p></div></article>)}</div><div className="admin-actions">{[[LibraryBig,'Manage materials','Create, edit, categorize, and remove resources.','/admin/materials'],[BookOpen,'Manage subjects','Maintain the academic catalog and categories.','/admin/subjects'],[ChartNoAxesCombined,'View reports','Explore activity, ratings, and recommendation insights.','/admin/reports']].map(([Icon,t,d,to])=><Link to={to as any} key={String(t)}><Icon/><div><h2>{t as string}</h2><p>{d as string}</p></div></Link>)}</div></div></AdminGuard>}
