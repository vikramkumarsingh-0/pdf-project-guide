import { createFileRoute,Link } from '@tanstack/react-router';import { useEffect,useState } from 'react';import { BookOpen,ChartNoAxesCombined,GraduationCap,Clock3,LibraryBig,Plus,Search,Star } from 'lucide-react';import { supabase } from '@/integrations/supabase/client';import { AdminGuard } from '@/components/study/admin-guard';import { Button } from '@/components/ui/button';
export const Route=createFileRoute('/_authenticated/admin/dashboard')({head:()=>({meta:[{title:'Admin dashboard | StudyFlow AI'},{name:'description',content:'StudyFlow administration and system overview.'},{property:'og:title',content:'Admin dashboard | StudyFlow AI'},{property:'og:description',content:'Manage learning resources and insights.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Admin});
function Admin(){const{user}=Route.useRouteContext();const[stats,setStats]=useState({materials:0,pending:0,subjects:0,ratings:0,searches:0});useEffect(()=>{Promise.all([supabase.from('materials').select('*',{count:'exact',head:true}),supabase.from('materials').select('*',{count:'exact',head:true}).eq('approval_status','pending'),supabase.from('subjects').select('*',{count:'exact',head:true}),supabase.from('ratings').select('*',{count:'exact',head:true}),supabase.from('search_logs').select('*',{count:'exact',head:true})]).then(([materials,pending,subjects,ratings,searches])=>setStats({materials:materials.count??0,pending:pending.count??0,subjects:subjects.count??0,ratings:ratings.count??0,searches:searches.count??0}))},[]);return <AdminGuard userId={user.id}><div className="page-wrap"><div className="page-title flex-row"><div><p className="eyebrow">Administration</p><h1>System overview</h1><p>Manage the catalog and monitor learning activity.</p></div><Link to="/admin/materials"><Button><Clock3/>Review queue</Button></Link></div><div className="stats-grid admin-stats">{[{Icon:Clock3,value:stats.pending,label:'Pending review'},{Icon:LibraryBig,value:stats.materials,label:'Materials'},{Icon:BookOpen,value:stats.subjects,label:'Subjects'},{Icon:Star,value:stats.ratings,label:'Ratings'},{Icon:Search,value:stats.searches,label:'Searches'}].map(({Icon,value,label})=><article className="stat" key={label}><span><Icon/></span><div><b>{value}</b><p>{label}</p></div></article>)}</div><div className="admin-actions"><Link to="/admin/materials"><LibraryBig/><div><h2>Review materials</h2><p>Validate submissions, publish trusted resources, and manage the catalog.</p></div></Link><Link to="/admin/subjects"><BookOpen/><div><h2>Manage subjects</h2><p>Maintain the academic catalog and categories.</p></div></Link><Link to="/admin/authors"><GraduationCap/><div><h2>Manage authors</h2><p>Edit educator profiles and connect trusted authors to learning resources.</p></div></Link><Link to="/admin/reports"><ChartNoAxesCombined/><div><h2>View reports</h2><p>Explore activity, ratings, and recommendation insights.</p></div></Link></div></div></AdminGuard>}
