import { createFileRoute,Link } from '@tanstack/react-router'
import { useEffect,useMemo,useState } from 'react'
import { useServerFn } from '@tanstack/react-start'
import { BookOpenCheck,CheckCircle2,Clock3,ExternalLink,GraduationCap,Send,Upload,XCircle } from 'lucide-react'
import { toast } from 'sonner'
import { supabase } from '@/integrations/supabase/client'
import { getEducatorPortal,submitEducatorMaterial,updateEducatorProfile } from '@/lib/educators.functions'
import { materialSubmissionSchema } from '@/lib/material-schemas'
import type { MaterialDoc } from '@/lib/recommendation'
import type { Subject } from '@/lib/catalog'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'
type Author={id:string;name:string;affiliation:string;biography:string;expertise:string[];website_url:string|null;image_url:string|null}
const blank={title:'',description:'',subjectId:'',type:'Article' as 'PDF'|'Video'|'Article',url:'',tags:''}
export const Route=createFileRoute('/_authenticated/educator')({head:()=>({meta:[{title:'Educator portal | StudyFlow AI'},{name:'description',content:'Submit teaching materials and manage your educator resources.'},{property:'og:title',content:'Educator portal | StudyFlow AI'},{property:'og:description',content:'Track verified teaching resources and submissions.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:EducatorPortal})
function EducatorPortal(){const{user}=Route.useRouteContext();const getPortal=useServerFn(getEducatorPortal);const submitMaterial=useServerFn(submitEducatorMaterial);const updateProfile=useServerFn(updateEducatorProfile);const[author,setAuthor]=useState<Author|null>(null);const[materials,setMaterials]=useState<MaterialDoc[]>([]);const[subjects,setSubjects]=useState<Subject[]>([]);const[form,setForm]=useState(blank);const[profile,setProfile]=useState({affiliation:'',biography:'',expertise:'',websiteUrl:'',imageUrl:''});const[file,setFile]=useState<File|null>(null);const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function load(){try{const[result,catalog]=await Promise.all([getPortal(),supabase.from('subjects').select('*').order('name')]);const a=result.author as Author;setAuthor(a);setMaterials(result.materials as MaterialDoc[]);setSubjects((catalog.data??[]) as Subject[]);setProfile({affiliation:a.affiliation,biography:a.biography,expertise:a.expertise.join(', '),websiteUrl:a.website_url??'',imageUrl:a.image_url??''})}catch(caught){setError(caught instanceof Error?caught.message:'Educator access required')}}useEffect(()=>{void load()},[]);const stats=useMemo(()=>({pending:materials.filter(x=>x.approval_status==='pending').length,approved:materials.filter(x=>x.approval_status==='approved').length,rejected:materials.filter(x=>x.approval_status==='rejected').length}),[materials]);async function saveProfile(e:React.FormEvent){e.preventDefault();try{await updateProfile({data:{...profile,expertise:profile.expertise.split(',').map(x=>x.trim()).filter(Boolean)}});toast.success('Educator profile updated');await load()}catch(caught){toast.error(caught instanceof Error?caught.message:'Could not update profile')}}async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);let filePath:string|null=null;try{if(file){if(!['application/pdf','video/mp4','video/webm'].includes(file.type)||file.size>15*1024*1024)throw new Error('Upload a PDF, MP4, or WebM file up to 15 MB');filePath=`${user.id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'-')}`;const upload=await supabase.storage.from('study-materials').upload(filePath,file,{contentType:file.type});if(upload.error)throw upload.error}const payload=materialSubmissionSchema.parse({...form,tags:form.tags.split(',').map(x=>x.trim()).filter(Boolean),authorId:null,filePath,fileName:file?.name??null,fileMimeType:file?.type??null,fileSizeBytes:file?.size??null});await submitMaterial({data:payload});toast.success('Teaching resource submitted for review');setForm(blank);setFile(null);await load()}catch(caught){if(filePath)await supabase.storage.from('study-materials').remove([filePath]);toast.error(caught instanceof Error?caught.message:'Could not submit resource')}finally{setBusy(false)}}if(error)return <div className="page-wrap narrow"><div className="empty-state"><GraduationCap/><h1>Educator access required</h1><p>Your account is not linked to a verified educator profile.</p><Link to="/educator/apply"><Button>Apply for access</Button></Link></div></div>;if(!author)return <div className="page-wrap"><div className="skeleton-block"/></div>;return <div className="page-wrap"><div className="educator-hero"><div>{author.image_url?<img src={author.image_url} alt={author.name}/>:<span className="author-avatar"><GraduationCap/></span>}</div><div><p className="eyebrow">Verified educator portal</p><h1>{author.name}</h1><p>{author.affiliation}</p></div></div><div className="stats-grid">{[[Clock3,stats.pending,'Pending'],[CheckCircle2,stats.approved,'Approved'],[XCircle,stats.rejected,'Rejected'],[BookOpenCheck,materials.length,'Linked resources']].map(([Icon,value,label])=><article className="stat" key={String(label)}><span><Icon/></span><div><b>{String(value)}</b><p>{String(label)}</p></div></article>)}</div><div className="educator-columns"><section><div className="section-heading"><div><p className="eyebrow"><Send/> New contribution</p><h2>Submit teaching material</h2></div></div><form className="submission-form" onSubmit={submit}><div><Label htmlFor="educator-title">Title</Label><Input id="educator-title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} required/></div><div><Label htmlFor="educator-description">Description</Label><Textarea id="educator-description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} minLength={20} required/></div><div className="submission-fields"><div><Label htmlFor="educator-subject">Subject</Label><select id="educator-subject" value={form.subjectId} onChange={e=>setForm({...form,subjectId:e.target.value})} required><option value="">Select subject</option>{subjects.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></div><div><Label htmlFor="educator-type">Type</Label><select id="educator-type" value={form.type} onChange={e=>setForm({...form,type:e.target.value as typeof form.type})}><option>PDF</option><option>Video</option><option>Article</option></select></div></div><div><Label htmlFor="educator-url">Resource link</Label><Input id="educator-url" type="url" value={form.url} onChange={e=>setForm({...form,url:e.target.value})}/></div><div><Label htmlFor="educator-tags">Tags</Label><Input id="educator-tags" value={form.tags} onChange={e=>setForm({...form,tags:e.target.value})} placeholder="algorithms, lecture, examples"/></div><div className="file-drop"><Upload/><div><Label htmlFor="educator-file">Or upload a file</Label><p>PDF, MP4, or WebM up to 15 MB</p></div><Input id="educator-file" type="file" accept="application/pdf,video/mp4,video/webm" onChange={e=>setFile(e.target.files?.[0]??null)}/></div><Button disabled={busy}><Send/>{busy?'Submitting…':'Submit for review'}</Button></form></section><section><div className="section-heading"><div><p className="eyebrow"><GraduationCap/> Public identity</p><h2>Edit educator profile</h2></div></div><form className="submission-form" onSubmit={saveProfile}><div><Label htmlFor="educator-affiliation">Affiliation</Label><Input id="educator-affiliation" value={profile.affiliation} onChange={e=>setProfile({...profile,affiliation:e.target.value})}/></div><div><Label htmlFor="educator-bio">Biography</Label><Textarea id="educator-bio" value={profile.biography} onChange={e=>setProfile({...profile,biography:e.target.value})}/></div><div><Label htmlFor="educator-expertise">Expertise</Label><Input id="educator-expertise" value={profile.expertise} onChange={e=>setProfile({...profile,expertise:e.target.value})}/></div><div><Label htmlFor="educator-website">Website</Label><Input id="educator-website" type="url" value={profile.websiteUrl} onChange={e=>setProfile({...profile,websiteUrl:e.target.value})}/></div><div><Label htmlFor="educator-image">Image URL</Label><Input id="educator-image" type="url" value={profile.imageUrl} onChange={e=>setProfile({...profile,imageUrl:e.target.value})}/></div><Button>Save profile</Button></form></section></div><section className="section-block"><div className="section-heading"><div><p className="eyebrow">Submission history</p><h2>Your linked resources</h2></div></div><div className="submission-history full-history">{materials.map(item=><article key={item.id}><div className={`status-icon status-${item.approval_status}`}>{item.approval_status==='approved'?<CheckCircle2/>:item.approval_status==='rejected'?<XCircle/>:<Clock3/>}</div><div><div className="submission-title-row"><h3>{item.title}</h3><span className={`status-badge status-${item.approval_status}`}>{item.approval_status}</span></div><p>{item.subjects?.name} · {item.type}</p>{item.rejection_reason&&<p className="rejection-note">Feedback: {item.rejection_reason}</p>}{item.approval_status==='approved'&&item.url&&<a className="text-link" href={item.url} target="_blank" rel="noreferrer">Open resource <ExternalLink/></a>}</div></article>)}</div></section></div>}
