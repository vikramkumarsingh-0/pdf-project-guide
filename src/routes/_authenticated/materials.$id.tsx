import { createFileRoute } from '@tanstack/react-router';import { useEffect,useState } from 'react';import { ExternalLink,Star } from 'lucide-react';import { toast } from 'sonner';import { supabase } from '@/integrations/supabase/client';import { openMaterial } from '@/lib/catalog';import type { MaterialDoc } from '@/lib/recommendation';import { Button } from '@/components/ui/button';
export const Route=createFileRoute('/_authenticated/materials/$id')({head:()=>({meta:[{title:'Study material | StudyFlow AI'},{name:'description',content:'View a trusted learning resource and student feedback.'},{property:'og:title',content:'Study material | StudyFlow AI'},{property:'og:description',content:'Learning resource details and reviews.'},{property:'og:type',content:'article'},{name:'twitter:card',content:'summary_large_image'}]}),component:Page});
function Page(){const{id}=Route.useParams();const{user}=Route.useRouteContext();const[material,setMaterial]=useState<MaterialDoc|null>(null);const[reviews,setReviews]=useState<any[]>([]);const[score,setScore]=useState(5);const[review,setReview]=useState('');const load=()=>Promise.all([supabase.from('materials').select('*,subjects(name)').eq('id',id).single(),supabase.from('ratings').select('*').eq('material_id',id).order('created_at',{ascending:false})]).then(([m,r])=>{setMaterial(m.data as MaterialDoc|null);setReviews(r.data||[])});useEffect(load,[id]);async function submit(e:React.FormEvent){e.preventDefault();const{error}=await supabase.from('ratings').upsert({user_id:user.id,material_id:id,score,review},{onConflict:'user_id,material_id'});if(error)toast.error(error.message);else{toast.success('Review saved');setReview('');load()}}if(!material)return <div className="page-wrap"><div className="skeleton-block"/></div>;return <div className="page-wrap narrow"><div className="material-detail-head"><p className="eyebrow">{material.subjects?.name} · {material.type}</p><h1>{material.title}</h1><p>{material.description}</p><div className="mt-5 flex flex-wrap gap-2">{material.tags.map(tag=><span className="tag" key={tag}>{tag}</span>)}</div><Button size="lg" className="mt-7" onClick={()=>openMaterial(material,user.id)}>Open resource <ExternalLink/></Button></div><section className="review-layout"><form onSubmit={submit}><h2>Rate this resource</h2><div className="rating-input">{[1,2,3,4,5].map(value=><button type="button" onClick={()=>setScore(value)} aria-label={`${value} stars`} key={value}><Star className={value<=score?'selected':''}/></button>)}</div><textarea value={review} onChange={e=>setReview(e.target.value)} placeholder="Share what helped you learn…" required minLength={5}/><Button>Save review</Button></form><div><h2>Student reviews</h2>{reviews.length?reviews.map(item=><article className="review" key={item.id}><div>{Array.from({length:item.score}).map((_,i)=><Star key={i}/>)}</div><p>{item.review||'Rated without a written review.'}</p><time>{new Date(item.created_at).toLocaleDateString()}</time></article>):<p className="mt-4 text-muted-foreground">Be the first to review this material.</p>}</div></section></div>}
