import { createFileRoute } from '@tanstack/react-router';import { useEffect,useMemo,useState } from 'react';import { RefreshCw,Sparkles } from 'lucide-react';import { getCatalog,openMaterial } from '@/lib/catalog';import { rankMaterials,type MaterialDoc } from '@/lib/recommendation';import { MaterialCard } from '@/components/study/material-card';import { Button } from '@/components/ui/button';
export const Route=createFileRoute('/_authenticated/recommendations')({head:()=>({meta:[{title:'Recommendations | StudyFlow AI'},{name:'description',content:'Explainable study recommendations personalized for you.'},{property:'og:title',content:'Recommendations | StudyFlow AI'},{property:'og:description',content:'See why each study resource was selected for you.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Recommendations});
function Recommendations(){const{user}=Route.useRouteContext();const[items,setItems]=useState<MaterialDoc[]>([]);const[tick,setTick]=useState(0);useEffect(()=>{getCatalog().then(x=>setItems(x.materials))},[tick]);const ranked=useMemo(()=>rankMaterials(items,'machine learning algorithms operating systems database web development',[]),[items]);return <div className="page-wrap"><div className="page-title flex-row"><div><p className="eyebrow"><Sparkles/>Explainable AI</p><h1>Made for your momentum</h1><p>Ranked using content relevance, learning signals, and quality.</p></div><Button variant="outline" onClick={()=>setTick(x=>x+1)}><RefreshCw/>Refresh</Button></div><div className="score-note"><b>How ranking works</b><span>Content match 50%</span><span>Peer signals 25%</span><span>Subject fit 15%</span><span>Quality 10%</span></div><div className="card-grid">{ranked.map(m=><div key={m.id} className="relative"><span className="score-pill">{Math.round(m.score*100)} match</span><MaterialCard material={m} reason={m.reason} onOpen={x=>openMaterial(x,user.id)}/></div>)}</div></div>}
